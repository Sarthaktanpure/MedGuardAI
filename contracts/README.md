# MedGuard Contracts

This directory contains the Solidity scaffold for the MedGuard batch registry.

The deployed address and generated ABI should be exported into `shared/types/` after a real deployment.
